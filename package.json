{
  "name": "music-to-piano",
  "version": "1.0.0",
  "description": "A convertor to convert a music to pure piano music.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "build": "node esbuild.config.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "piano"
  ],
  "author": "EternalHeart",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "dependencies": {
    "fluent-ffmpeg": "^2.1.2",
    "web-audio-api": "^0.2.2"
  }
}
